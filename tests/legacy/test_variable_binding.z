// Test variable binding in match patterns
fn test_variable_binding() -> i64 {
    let x = 42;
    
    // This should bind y to the value of x (42)
    match x {
        y => y + 1  // Should return 43
    }
}

fn main() -> i64 {
    test_variable_binding()
}