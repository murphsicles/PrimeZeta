fn main() -> i64 {
    let a = 5;
    let b = 10;
    let c = 15;
    
    // Test multiple comparisons
    if a < b && b < c {
        return 1; // Should return 1
    } else {
        return 0;
    }
}