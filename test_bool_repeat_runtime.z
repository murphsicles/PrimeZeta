// Test bool array repeat at runtime (no subscript, just create and return)
fn main() -> i64 {
    // This should create [false; 3]
    let arr: [bool; 3] = [false; 3];
    
    // Return 42 if it compiles and runs
    42
}