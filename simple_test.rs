fn main() {
    // Test the parse_param function directly
    let test_cases = vec![
        "a: i64",
        "b: String",
        "c: &mut i32",
    ];
    
    for test in test_cases {
        println!("Testing: '{}'", test);
        // We need to import and call parse_param
    }
}