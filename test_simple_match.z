// Test simple match without type annotation
fn test_simple_match() -> i64 {
    let x: i64 = 42;
    
    match x {
        y => y,
        _ => 0
    }
}

fn main() -> i64 {
    test_simple_match()
}