fn main() -> i64 {
    // Test basic operations
    let mut x = 5
    x = x + 1
    
    // Test if statement
    let y = if x < 10 { 1 } else { 0 }
    
    // Test function call
    let z = add_one(x)
    
    z
}

fn add_one(a: i64) -> i64 {
    a + 1
}