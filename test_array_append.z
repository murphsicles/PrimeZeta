// Test array append functionality
fn main() -> u64 {
    // Test 1: Create array and append values
    let mut arr = [0, 0, 0]  // Fixed-size array
    
    // Try to append (this should work with our fix)
    // arr.append(42)  // This would test append_i64
    
    // For now, just test array access
    arr[0] = 10
    arr[1] = 20
    arr[2] = 30
    
    // Return sum to verify array works
    arr[0] + arr[1] + arr[2]
}