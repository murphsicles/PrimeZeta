// Test i64 array repeat pattern with subscript
fn main() -> i64 {
    // This should create [7, 7, 7]
    let arr: [i64; 3] = [7; 3];
    
    // Access elements
    let mut sum: i64 = 0;
    sum += arr[0];
    sum += arr[1];
    sum += arr[2];
    
    sum  // Should be 21
}