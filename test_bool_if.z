fn main() -> i32 {
    let mut count: i32 = 0;
    
    for n in 2..10 {
        let is_prime: bool = true;
        count += 1; // Count loop iterations
        if is_prime {
            // Do nothing
        }
    }
    
    return count; // Should return 8
}