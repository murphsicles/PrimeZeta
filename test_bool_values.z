fn main() -> i64 {
    let b1: bool = true;
    let b2: bool = false;
    
    // Convert bool to i64 directly
    let v1: i64 = b1 as i64;
    let v2: i64 = b2 as i64;
    
    v1 * 10 + v2  // Should be 10 (1*10 + 0)
}