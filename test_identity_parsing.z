// Test identity type parsing in type checker
fn main() {
    let x: string[identity:read] = "hello";
    let y: string = "world";
    
    // Test pattern matching with identity types
    match x {
        s: string[identity:read] => println(s),
        _ => println("not a readable string")
    }
}