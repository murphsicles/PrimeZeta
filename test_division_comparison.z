fn main() -> i64 {
    // Test d <= n / d comparison
    let n = 25
    let mut d = 1
    let mut count = 0
    
    while d <= n / d {
        count = count + 1
        d = d + 1
    }
    
    return count
}