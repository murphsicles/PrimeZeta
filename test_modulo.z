fn main() -> i32 {
    let mut count: i32 = 0;
    
    for n in 2..10 {
        if n % 2 == 0 {
            count += 1;
        }
    }
    
    return count; // Should return 4 (2, 4, 6, 8 are divisible by 2)
}