fn main() -> u64 {
    // Use dynamic array which we know works
    let arr: [dynamic]u64 = [dynamic]u64{}
    arr.push(1)
    arr.push(2)
    arr.push(3)
    arr.push(4)
    arr.push(5)
    
    // Return sum
    return arr[0] + arr[1] + arr[2] + arr[3] + arr[4]
}