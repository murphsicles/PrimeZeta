// Test if comparison operators are implemented
fn main() -> u64 {
    let a = 5u64
    let b = 10u64
    
    // Test all comparison operators
    if a < b {
        return 1  // Should return 1 if < works
    }
    
    return 0
}