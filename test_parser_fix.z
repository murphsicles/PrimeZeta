// Test parser fix for string[identity:read] without whitespace
fn test_parser() -> i64 {
    let x: string[identity:read] = "hello";
    let y: string [identity:read] = "world";  // with space
    42
}

fn main() -> i64 {
    test_parser()
}