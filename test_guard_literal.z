// Test guard clauses with literal patterns
fn main() -> i64 {
    let x = 3;
    
    match x {
        1 if false => 100,     // Won't match (pattern matches but guard fails)
        3 if true => 200,      // Should return 200
        _ => 0
    }
}