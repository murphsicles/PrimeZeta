// Test which operators work in current Zeta
fn main() -> u64 {
    // Test basic arithmetic
    let a: u64 = 10
    let b: u64 = 3
    
    // Test addition
    let sum: u64 = a + b
    
    // Test subtraction  
    let diff: u64 = a - b
    
    // Test multiplication
    let prod: u64 = a * b
    
    // Test division
    let quot: u64 = a / b
    
    // Test modulo
    let rem: u64 = a % b
    
    // Test comparisons (if they work)
    // let gt: bool = a > b
    // let lt: bool = a < b
    // let eq: bool = a == b
    
    // Test bitwise (if they work)
    // let bit_and: u64 = a & b
    // let bit_or: u64 = a | b
    // let shift_left: u64 = a << 1
    // let shift_right: u64 = a >> 1
    
    // Return something to test
    return sum
}