// Test field access with different values
struct Point {
    x: i64,
    y: i64
}

fn test_field_access() -> i64 {
    let p = Point { x: 5, y: 15 };
    p.x + p.y  // Should return 20
}

fn main() -> i64 {
    test_field_access()
}