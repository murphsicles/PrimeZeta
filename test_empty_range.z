fn main() -> i32 {
    let mut sum: i32 = 0;
    
    // Test empty range
    for i in 2..2 {
        sum += 1;
    }
    
    // Test range with one element
    for i in 2..3 {
        sum += 1;
    }
    
    return sum; // Should return 1
}