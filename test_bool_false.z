fn main() -> i32 {
    let mut count: i32 = 0;
    
    for n in 2..10 {
        let is_prime: bool = false;
        if is_prime {
            count += 1;
        }
    }
    
    return count; // Should return 0
}