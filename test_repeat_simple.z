// Simple array repeat test
fn main() -> i64 {
    let arr: [bool; 3] = [false; 3];
    
    // Return 1 if first element is false (should be)
    if arr[0] == false {
        1
    } else {
        0
    }
}