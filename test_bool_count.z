fn main() -> i32 {
    let mut count: i32 = 0;
    
    for n in 2..10 {
        let is_prime: bool = false;
        count += 1; // Count loop iterations
    }
    
    return count; // Should return 8
}