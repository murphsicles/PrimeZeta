fn main() -> i32 {
    let limit: i32 = 5;
    let mut sum: i32 = 0;
    
    // Test with variable
    for i in 0..limit {
        sum += i;
    }
    
    // sum should be 0+1+2+3+4 = 10
    return sum;
}