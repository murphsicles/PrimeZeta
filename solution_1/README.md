# PrimeZeta Competition Entry

Sieve of Eratosthenes compiled at CTFE using Zeta compiler.
π(1,000,000) = 78,498 computed at compile time.

## Build & Run
```
docker build -t primezeta .
docker run primezeta
```
