// This is the debug_array_bug.z test case
// With the runtime fix, it should return 15
fn main() -> i64 {
    let arr: [i64; 5] = [1, 2, 3, 4, 5];
    let mut sum: i64 = 0;
    let mut i: i64 = 0;
    while i < 5 {
        sum += arr[i as usize];
        i += 1;
    }
    sum
}