// Test bool array repeat with assignment (not read)
fn main() -> i64 {
    // This should create [false; 3]
    let mut arr: [bool; 3] = [false; 3];
    
    // Set some elements
    arr[0] = true;
    arr[2] = true;
    
    // Return 42 if it works
    42
}