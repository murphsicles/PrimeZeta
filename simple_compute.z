// Simple computation test
fn main() -> u64 {
    let mut sum: u64 = 0
    let mut i: u64 = 0
    
    // Try to sum numbers from 0 to 9
    while i < 10 {
        sum = sum + i
        i = i + 1
    }
    
    return sum  // Should return 45
}