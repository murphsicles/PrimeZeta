fn main() -> i64 {
    let arr: [bool; 3] = [true, false, true];
    let mut sum: i64 = 0;
    
    for i in 0..3 {
        if arr[i] {
            sum += 1;
        }
    }
    
    sum  // Should be 2
}