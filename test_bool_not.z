fn main() -> i64 {
    let b1: bool = true;
    let b2: bool = false;
    
    let mut result: i64 = 0;
    if !b1 { result += 1; }  // Should not add
    if !b2 { result += 2; }  // Should add 2
    
    result  // Should be 2
}