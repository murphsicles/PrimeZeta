// Test CTFE - compute at compile time
fn main() -> u64 {
    comptime {
        // Try to compute something at compile time
        let x = 2 + 3
        return x
    }
    
    // Fallback if comptime doesn't work
    0
}