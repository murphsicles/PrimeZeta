// Simple test to check Zeta syntax
fn main() -> u64 {
    let limit: u64 = 100;
    var count: u64 = 0;
    
    var i: u64 = 2;
    while (i < limit) {
        count += 1;
        i += 1;
    }
    
    return count;
}